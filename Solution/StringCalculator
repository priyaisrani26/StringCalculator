package Solution;

public class StringCalculator{
  public int Add(String numbers) {
	  if(numbers.isEmpty()) {
		  return 0;
	  }
	  if(numbers.length() == 1) {
		  return numbers.length();
	  }
	  return 0;
  }
	
}

